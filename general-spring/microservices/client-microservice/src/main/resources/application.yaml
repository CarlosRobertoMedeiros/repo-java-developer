spring:
  application:
    name: client-microservice

  datasource:
    url: jdbc:postgresql://localhost:5432/client-db
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
